import React from "react"
import { TextInput } from "react-native"

const SearchBox = () => {
    
    return (
        <TextInput value=' aom ...Search for a collection here..'/>
    );
    
}

export default SearchBox;


{/* <TextInput onChangeText={text => this.props.onChangeCriteria(text)}/> */}