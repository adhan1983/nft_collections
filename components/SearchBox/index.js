import React from "react"
import { TextInput } from "react-native"

const SearchBox = () => {
    
    return (
        <TextInput value='Search for a collection here..'/>
    );
    
}

export default SearchBox;
